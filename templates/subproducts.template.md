# Sub-Products - {{MAIN_NAME}}

**Updated:** {{UPDATED_DATE}}
**Main workspace:** `{{MAIN_PATH}}`
**Sub-products:** {{SUB_COUNT}}

Read-only roll-up of every sub-product workspace linked to this main product.
Regenerated by `loop session-start` / `loop workspace refresh`. Never edit by hand -
edit the sub-product's own files, or the links in `.loop/workspace.json`.

## Sub-products

{{SUMMARY_TABLE}}

## Drift vs the master plan

{{FINDINGS}}

## Detail

{{DETAILS}}

## Rules

- Sub-product state is **read** from here, never written. Corrections are staged into
  the sub-product's `.loop/pending/` and applied there with `loop pending approve --all`.
- A sub-product's row is only as fresh as its **last session** column.
- Bind each sub-product to a `plan/PRODUCT_MAP.md` row - an unmapped sub-product is
  work the master plan cannot account for.

## Next

{{NEXT_ACTIONS}}
